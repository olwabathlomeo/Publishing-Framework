\documentclass[11pt]{article}
\usepackage{fontspec}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue}

% Provide tightlist if pandoc uses it for compact lists
\providecommand{\tightlist}{%
	\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}%
}

$if(title)$
\title{$title$
\vspace{-1em}}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \\ $endfor$}
$endif$
$if(date)$
\date{$date$}
$else$
\date{}
$endif$

\begin{document}
\maketitle
\vspace{1em}

$body$

\end{document}
